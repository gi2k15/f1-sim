"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getRaceResults = void 0;
const axios_1 = __importDefault(require("axios"));
const cheerio = __importStar(require("cheerio"));
const endpoints_1 = require("../endpoints/endpoints");
const getRaceResults = async (year = new Date().getFullYear()) => {
    try {
        let raceResults = [];
        const response = await (0, axios_1.default)(`${endpoints_1.dynamicLinks.rootLink}/${year}/${endpoints_1.dynamicLinks.results}`);
        const $ = cheerio.load(response.data);
        $(".f1-table > tbody:nth-child(2) > tr").each(function () {
            const grandPrixAll = $(this).find("td:nth-child(1) > p:nth-child(1) > a:nth-child(1)").contents().get()[1];
            const raceDate = $(this).find("td:nth-child(2) > p:nth-child(1)").text().trim();
            const driverName = $(this).find("td:nth-child(3) > p:nth-child(1) > span:nth-child(1)").text().trim();
            const winner = driverName.slice(0, driverName.length - 3).replace(/\u00a0/g, " ");
            const car = $(this).find("td:nth-child(4) > p:nth-child(1)").text().trim();
            const laps = parseInt($(this).find("td:nth-child(5) > p:nth-child(1)").text().trim());
            const time = $(this).find("td:nth-child(6) > p:nth-child(1)").text().trim();
            const date = new Date(Date.parse(`${raceDate} ${year} 00:00:00 GMT`));
            const grandPrix = $(grandPrixAll).text();
            if ((grandPrix.length !== 0 && raceDate.length !== 0 && winner.length !== 0, car.length !== 0, !Number.isNaN(laps), time.length !== 0)) {
                const raceResult = {
                    grandPrix,
                    date,
                    winner,
                    car,
                    laps,
                    time,
                };
                raceResults.push(raceResult);
            }
        });
        if (raceResults.length === 0) {
            throw new Error(" No data found");
        }
        return raceResults;
    }
    catch (error) {
        throw new Error(error);
    }
};
exports.getRaceResults = getRaceResults;
//# sourceMappingURL=race-results.js.map